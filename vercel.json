{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npx prisma db push --accept-data-loss && npx prisma db execute --file prisma/rls.sql --schema prisma/schema.prisma && npm run build",
  "crons": [
    {
      "path": "/api/cron/stock-alerts",
      "schedule": "0 3 * * *"
    }
  ]
}
